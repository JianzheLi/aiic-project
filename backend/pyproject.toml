[project]
name = "aiic-agent-backend"
version = "0.1.0"
description = "FastAPI backend for a 16-hour AI Agent product challenge MVP."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1.0",
    "openai>=1.59,<3.0",
    "uvicorn[standard]>=0.34,<1.0",
]

[tool.uv]
package = false
